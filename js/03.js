/*3. Escreva um código em JavaScript que cria um objeto chamado "carro" com as seguintes propriedades: "marca" (string),
"ano" (number) e "ligado" (boolean). Em seguida, crie um método chamado "ligar" que altera o valor da propriedade
"ligado" para true. Teste o método e exiba o objeto "carro" no console. */
/*
let carro = {
    marca: "Fiat",
    ano: 2006,
    ligado: false,
    ligar: function(){
        this.ligado = true
    }
}
carro.ligar()
console.log(carro)
*/
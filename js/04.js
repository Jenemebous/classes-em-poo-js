
/* 4. Escreva uma função em JavaScript chamada "adicionarPropriedade" que recebe um objeto, uma chave (string) e um valor
como parâmetros. A função deve adicionar uma nova propriedade ao objeto com a chave e o valor fornecidos. Retorne o
objeto modificado. */

/*
function adicionarPropriedade(obj, chave, valor){
  obj[chave] = valor;
  return obj;
}
var meuObjeto = { nome: "Cassandra" };
adicionarPropriedade(meuObjeto, "idade", 42);
console.log(meuObjeto);
*/


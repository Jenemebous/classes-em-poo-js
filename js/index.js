/*01 1. O código JavaScript abaixo não está retornando o resultado esperado. Identifique o problema e faça as alterações
necessárias para que o código funcione corretamente: */

/*function calcularMedia(numeros) {
    let soma = 0;
    for (let i = 0; i < numeros.length; i++) {
        soma += numeros[i] / 4;
        
    }
    return soma;
}
let numeros = [5, 10, 15, 20];
let media = calcularMedia(numeros);
console.log("A média é " + media); */